# TO-DO

> 갱신 규칙: 완료되면 체크 후 `progress.md`로 요약을 옮긴다. 세션 시작 시 이 파일부터 본다.
> 완료 항목은 여기 쌓아두지 말 것 — 이 파일은 **앞으로 할 일만** 남긴다.
> 최종 정리: 2026-08-04
> **확정된 규칙은 `docs/decisions.md`** — 여기는 앞으로 할 일만.

## 지금 막고 있는 것 — 대표 확인

- [ ] **OMR 지면 전제 — 손글씨가 아니라 마킹이었다**(2026-08-04 샘플 실물 확인).
  추가 마킹란도 카드에 없다. 아래 OMR 절 참조 — 인식 방식 전체가 여기서 갈린다
- [ ] **Apple Developer 가입 + FairPlay Streaming 신청** ← 리드타임이 제일 길다(승인 수일).
  재생이 전부 웹인데 iOS 는 브라우저 무관 WebKit 이라 Widevine 이 안 돈다.
  **D-U-N-S 는 회사에 이미 있음**(2026-07-30 확인) → 법인 계정 며칠이면 된다
- [ ] **Mux 계정만 무료로 개설**(돈 안 든다) — 전송 10만 분 무료라 실제 강의 영상 한두 개로
  **한국 재생 품질**을 지금 확인할 수 있다. 단 **본 라이브러리는 아직 올리지 말 것**(아래 참조)
- [ ] **알림톡 발송 시점 전체 리스트**(PRD 8-17) — 가장 오래 막혀 있다. 카카오 템플릿 사전 승인제라 문구까지 확정돼야 개발 시작.
  **2026-08-05: 발송 파이프라인은 완성됐다**(`notifications-sending` 브랜치) — 목록이 오면 트리거만 붙인다
- [ ] **알리고 API 키·user_id·발신번호·발신프로필키** — 대표가 이미 알리고를 쓰고 있어 업체를 알리고로 확정(`docs/decisions.md` §3-1).
  대표 계정을 같이 쓸지 별도로 팔지도 대표 결정. **키가 오면 `_request` 한 곳만 구현하면 발송이 열린다**
- [ ] **채널톡 도입 컨펌 + 플러그인 키** — 랜딩의 유일한 전환 경로
- [ ] **채널톡 영업에 한 가지만 확인** — *"우리 서버에서 API 로 특정 1명에게 알림톡을
  즉시 보내고, 건별 발송 결과를 돌려받을 수 있나?"* 공개 문서에는 그 경로가 없다
  (수신자를 CSV·고객태그·사이트 행동으로 고르는 마케팅 도구다). **가능하다면** 업체를
  채널톡으로 합칠 수 있다 — 어댑터 파일 1개 교체라 비용은 거의 없다.
  안 되면 알리고 유지(아래 근거는 `docs/2026-08-05-알림발송-비용과-업체선정.md`)
- [ ] 결제선생 연동 스펙

## 진행 중

- [ ] **사용자 직접 테스팅** ← **지금 여기.** 실앱(http://localhost:5173) 전 기능 순회.
  프런트는 7/29 새벽 감사·클리닉 재작성까지 끝났다(progress.md 참조) — 다음 우선순위를 정하는 근거가 이 테스팅이다.
  비번 전부 `test1234` · 학생·학부모는 `/login`, 직원은 `/login/admin`

  | 계정 | 쓰임 |
  |---|---|
  | `한종철0001` 대표 | 직원 권한 매트릭스는 여기서만 보인다 |
  | `김관리0002` 관리자 / `박조교0003` 조교 | 조교는 권한이 좁아 메뉴가 줄어든다(게이팅 확인) |
  | `김하늘0001` 학생 | 수요반 · 클리닉 대상(3회차) |
  | `강태윤0026` 학생 | 클리닉 대상 — 김하늘이 잡은 칸이 여기선 "마감"으로 보인다 |
  | `장예준0029`·`임다인0030` | **예비등록** — 홈이 거의 비어 있다(상태 기반 노출 확인) |
  | `김하늘0001p` 학부모 | 자녀 2명(전환 UI) / `정서연0005p` 는 1명 |

  아이디 규칙은 학생 `{이름}{번호4자리}`(0001~0030), 학부모 `{자녀 아이디}p`.

## OMR (2026-08-04 착수, 별도 에이전트 트랙)

**테스팅 대기에서 뺐다**(사용자 지시 2026-08-04). 샘플 카드 실물이 있어
"실제 화면을 본 뒤 착수"가 막던 것이 풀렸다 — 지면이 곧 스펙이다.
샘플 `~/Desktop/assets/test OMR.pdf` (2027 OMEGA black 3회 · 튜터시스템 제작)

### 실물이 문서 전제를 뒤집는다 — 대표 확인 필요

| | 문서 전제 | 샘플 카드 |
|---|---|---|
| 이름 | 손글씨 기입 | **자모 마킹**(4글자 격자, "좌측부터 차례로 마킹") |
| 전화 뒷4 | 손글씨 기입 | **숫자 마킹**(4칸 × 0~9, "0 있는 경우에도 모두 마킹") |
| 추가 마킹란 | 문항번호 옆 색칠 박스(PRD 3.1.8) | **없다** — 답란은 20문항 × 5지선다뿐 |

`unique_id.py` 가 "**OMR 마킹이 아니라 손글씨 전제다**"(2026-07-29 대표 *"OMR은 이름
뒷자리만 들어올 것 같아"*)라고 못 박고 있고, `decisions.md` §5 의 "지면 값은 예비값"도
그 위에 서 있다. **원번 = `{이름}{뒷4}` 는 그대로 성립한다** — 카드가 둘 다 받는다.
바뀌는 건 판독 신뢰도이고 방향이 좋은 쪽이다:

- 손글씨 OCR 이 필요 없다 → **외부 인식 엔진 대기가 소멸할 수 있다**(마킹 검출로 끝난다)
- 6분기의 `비정상`·`부분` 빈도가 크게 준다. `중복`(동명이인+같은 뒷4)은 그대로 남는다
- 대신 **한글 자모 → 음절 조합**을 우리가 짜야 한다(칸마다 초·중·종성 열)
- 워크북은 여전히 조교 손글씨라 OCR 전제 유지 — 두 지면이 갈린다

### 할 일

- [ ] **추가 마킹란 없는 카드를 어떻게 할지** — PRD 3.1.8 약점체크의 입력 절반이다.
  지면 재인쇄인지, 1차는 오답만으로 가는지
- [ ] **문항 정보 입력(정답·배점·단원·학습가이드)** — 채점의 선행 조건. 정답이 없으면 채점이 성립하지 않는다
- [ ] 인식 결과 수용 계약(`apply_recognition` 자리)을 먼저 판다 — 엔진과 독립. 워크북 선례
- [ ] 마킹 검출 — 네 모서리 정렬 마커로 기울기·배율 보정 후 격자 샘플링
- [ ] 대조 6분기 매칭 — 워크북 4종을 복사하지 말 것(저기는 "1명 일치 아니면 전부 불일치")
- [ ] 스캔 업로드 → 자동 채점 → 보정 UI(스캔 이미지 ↔ 인식 답안 나란히 — PRD 3.1.1)
- [ ] `Attendance.exam_taken` vs `Score.is_taken` 결정 — `models.py` 에 "잠정"으로 남아 있고 클리닉 판정이 여기 매달려 있다
- [ ] 시험 식별 — 카드 상단이 `2027 OMEGA black 3회` 글자뿐이다. 업로드 때 관리자가 고르는지 지면에서 읽는지

## 랜딩 (2026-07-28 착수, 별도 트랙)

스펙 `frontend/landing/SPEC.md` · 자산 `frontend/landing/assets/INDEX.md` · 조사 `local/landing-refs/`
히어로·3섹션 구현 완료(`e012b00`), 타이포 SUIT 900(`e4eb447`). 로컬에서 도는 정적 페이지다.
**배경은 7/29 에 먼지밭으로 확정, 모티프판 폐기**(`95d6b91` — 기록은 SPEC §4-옛).

- [ ] **히어로 비주얼 교체** — 모티프판을 걷어낸 자리가 비어 있다(다른 비주얼 예정).
  `field.js` 의 LAYOUT·TONE·bake·손전등과 CFG 의 flash·soft·ink·lag 는 새 비주얼이 그 자리를
  쓸 수 있어 코드에 남겨 뒀다. 되살릴 때 디버그 노브 그룹도 함께 복구
- [ ] **채널톡 플러그인 키** — `chat.js`의 `window.__CHANNEL_TALK_KEY__` 주입 자리만 있음. 키 없으면 위젯이 안 뜬다. 랜딩의 **유일한 전환 경로**라 이게 비면 상담 신청이 불가능(PRD 3.3.3)
- [ ] **커리큘럼 강좌 라인업 실제 값** — `data.js`에 기존 교재로 임시 배치. 4단계 × 2트랙 축에 실제 라인업 재배치 필요
- [ ] **정오표 데이터 소스** — 지금 빈 배열. `boards`의 `Post.Category.ERRATA`를 비로그인 공개 API로 열어야 하고, 웹 표를 그리려면 `errata_items` 자식 테이블이 필요(현 `Post`는 title/body뿐). `Post.updated_at`이 `auto_now` 없는 nullable이라 갱신 지점 명시 안 하면 정렬이 무너짐
- [ ] **배포 경로 미정** — 랜딩을 어디에 띄울지(Fly 정적 서빙 / 별도 호스팅 / LMS 라우트 편입). nav 로그인이 `/student/login`을 가리키므로 같은 도메인이 자연스러움

## 다음 (테스팅 피드백 받은 뒤)

- [ ] PRD 3장 대비 **API 미구현 목록**(2026-07-29 실사 — 현재 라우트 68개). 우선순위는 테스팅 후 결정:
  - ~~OMR 스캔 업로드·자동 채점·보정 UI~~ → **별도 에이전트 트랙으로 선행 착수**(위 OMR 절).
    문항 정보 입력도 채점의 선행 조건이라 함께 간다
  - 문제은행·유사문항 매칭 (모델만 있고 API 0건)
  - 성적표·약점체크 PDF 생성 (0건)
  - 학부모 상담 신청 (0건 — PRD 상 학부모 write 허용 3종 중 하나)
  - 퇴원 처리 (`Student.enrollment_status`·`withdrawn_*` 필드는 있고 API 만 없음)
  - ~~관리자 발송내역 조회~~ **완료(2026-08-05)** — `GET /api/admin/notifications`(필터·페이징) +
    `POST /api/admin/notifications/{id}/resend`. **화면은 아직 없다** / 교재 청구 개시 / 시험·상담 목록 서버 필터·페이징
- [ ] PRD 8-14: **1차 오픈 범위** 확정 — 개발 속도 나왔으니 이번 주 결정 가능
- [ ] **원번 = 로그인 아이디 통합** — 접미사까지 같게. 그러면 원번도 유일해진다.
  지면 인식값은 접미사가 없으니 **접두 일치로 후보를 찾고 조교가 고른다**
  (대표 2026-07-30: "OMR인식값은 헷갈리면 ui로 띄워주고 조교가 매칭")
- [ ] **로그인 아이디 접미사 두 글자 확장** — 25개 소진 시 지금은 발급이 실패한다.
  `aa`·`ab`… 로 열되 **끝자리가 `p` 인 것은 건너뛴다**(`ap` 는 `…a` 학생의 학부모 자리)
- [ ] **워터마크 진하기·주기 조절** — 지금 opacity 0.32 · 48초 네 모서리.
  `frontend/src/pages/student/video.css` 한 곳에 있다. 대표가 실제 강의 영상으로 보고 정할 값
- [ ] **학부모도 복습영상을 보나** — 지금은 학생 전용(`/student/videos`). 미정
- [ ] **현보 학생 명단 편입** — 다른 수업 학생이 우리 회차에 오면 원래 명단에 없다. 어떻게 띄울지 미정
- [ ] **졸업 처리** — N수(학년 4) 다음이 없다

## 돈이 나가는 시점 — 런칭 직전에 (2026-07-30 대표: "아직 돈내기 싫어서")

- [ ] **Mux DRM 액세스 요청** — 유료 플랜 업그레이드가 요청 조건이라 **이때부터 월 $100**.
  같이 물을 것: **선불 크레딧($20 → $100)이 DRM 접근료에 적용되는지** — 월 11만원이냐 17만원이냐가 갈린다
- [ ] **본 영상 라이브러리 업로드** — DRM 켠 뒤에 해야 한다.
  Mux 는 무료 등급(`basic`)에 **DRM 을 걸 수 없어** `plus` 로 올려야 하고,
  먼저 basic 으로 올려두면 **전부 다시 올려야 한다**(4K 100시간 재인코딩 약 $600)

## 배포 전 (반나절 거리, 첫 실배포 직전 일괄)

- [ ] prod `SECRET_KEY` fail-fast(기본값 제거), whitenoise + collectstatic(현재 /static/ 404)
- [ ] `infra/Dockerfile`에 `ENV UV_NO_DEV=1`(부팅 13초 지연), uv 이미지 태그 고정
- [ ] CI `FLY_API_TOKEN` 시크릿(현재 미설정이라 자동배포 실행 자체가 안 됨)
- [ ] Fly `edujc-lms` 헬스체크 critical 상태 원인 확인
- [ ] **Sentry — sentry.io 가입해서 DSN 받아오기.** 사람만 할 수 있는 건 이것 하나다.
  절차·검증은 `infra/DEPLOY.md` **8장**에 다 적어 뒀다(가입 → 플랫폼 Django → Client Keys
  에서 DSN 복사 → `fly secrets set SENTRY_DSN="<DSN>" -a edujc-lms` → `/sentry-debug` 로
  실제 500 한 건 내서 대시보드에 뜨는 것까지 확인 → 확인용 토큰 회수).
  `fly ext sentry create`는 **불가**(7/28 확인 — Fly 경유 신규 통합 중단).
  **동기**: `fly logs`가 ~30분만 남아 "어제 왜 500났지"를 사후 추적 못 함(7/28 qbank 500 조사에서 실제로 막힘)
  - 코드는 2026-08-04 에 마무리했다: `config/observability.py` 로 분리 + 개인정보 차단
    (요청 본문·쿼리 값·스택 지역변수 — `send_default_pii=False` 만으로는 안 막힌다),
    DSN 오타에 부팅 안 죽게, 계약은 `config/tests.py` 14건이 지킨다.
    **`config/` 테스트는 `manage.py test apps` 에 안 잡힌다 — `test apps config` 로 돌릴 것**
- [ ] **Celery 워커 + Redis 기동 — 아직 아님**(2026-07-22 결정, 보류 유지). `@shared_task` 0건이라 할 일이 없고 워커는 auto_stop 대상이 아니라 24시간 돌며 월 ~$3.3. **해제 트리거 = 첫 `@shared_task` 작성 시점**(알림톡 발송 또는 영상 처리). 복구 3단계와 경위는 `infra/DEPLOY.md` 6장

## 외부 대기 (오는 대로 붙임 — 자리는 다 파여 있음)

- [x] ~~솔라피 연동 + 첫 Celery 태스크~~ → **파이프라인 완성(2026-08-05, `notifications-sending`)**.
  채널 어댑터·발송 태스크·재시도·상태 전이·재발송 배치·발송내역 조회 API·개인별 재발송까지.
  업체는 **알리고**로 확정(솔라피에서 변경 — `docs/decisions.md` §3-1). 남은 것은 ①알리고 키 4개
  ②발송 시점 목록(8-17) ③`aligo.py` 의 `_request` HTTP 한 곳 ④워커 기동(`infra/DEPLOY.md` 6장)
- [ ] **박 대표 컨펌**: UI 방향 (0721 회의에서 당일 컨펌 예정이었음). 영상 업체는 2026-07-30 Mux 확정
- [ ] Meet API(현재 meet_url 수동 입력). **OMR 인식 엔진은 여기서 뺐다** — 실물이 마킹
  카드라 외부 엔진 없이 갈 수 있다(위 OMR 절)
- [ ] **Carbon MCP 인바이트** — IBM 승인 대기(7/22 요청). 코드 오면 `claude mcp add-json carbon-mcp ...` → 세션 재시작. **단 현재 디자인은 hallmark 네이비로 확정됐으므로 참고용으로 격하**
