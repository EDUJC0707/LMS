# 프런트엔드 환경변수. frontend/.env 로 복사해서 사용.
#
# API 주소는 여기 없다 — axios 는 `/api` 고정이고(src/api/http.ts) 개발 중에는
# vite 프록시가 백엔드로 넘긴다. 절대 주소를 박으면 배포에서 브라우저가 엉뚱한
# 호스트를 부른다(vite.config.ts 주석).

# 프록시가 넘길 백엔드. 세션마다 포트를 나눠 쓸 때만 지정한다(CLAUDE.md §9).
# VITE_API_TARGET=http://127.0.0.1:8010
